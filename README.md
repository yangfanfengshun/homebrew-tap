# Homebrew Tap

SpecWeaver 的 Homebrew tap。**内容由主仓库自动生成，请勿直接修改**——
cask 模板的真相源在 `packaging/homebrew/specweaver.rb.tmpl`。

```bash
brew install --cask yangfanfengshun/tap/specweaver
```

升级：

```bash
brew upgrade --cask specweaver
```
